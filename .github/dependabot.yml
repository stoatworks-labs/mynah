# Keeps dependencies current across the fleet.
#
# Minor and patch bumps are grouped into a single PR per ecosystem per week, so
# routine drift costs one review rather than twenty. Majors still arrive as
# their own PRs, because they need reading.
#
# Deliberately no `ignore:` rules — an ignore also suppresses the *security*
# update for that dependency, which is exactly the PR worth having.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 3
    groups:
      github-actions-minor-and-patch:
        patterns: ["*"]
        update-types: ["minor", "patch"]
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      npm-minor-and-patch:
        patterns: ["*"]
        update-types: ["minor", "patch"]
      # React and its type packages must move together, majors included.
      # Ungrouped, Dependabot raises the runtime bump and the @types bump as
      # separate PRs, and each one is internally inconsistent: @types/react@19
      # against @types/react-dom@18 fails `npm ci` on a peer conflict before a
      # single test runs. That reads as "React 19 breaks the app" when nothing
      # has actually been tested. Grouping them produces one PR that CI can
      # give a real answer about.
      #
      # This is a group, not an `ignore` — see the note above; an ignore would
      # also suppress security updates for React, which is the PR worth having.
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
        update-types: ["major", "minor", "patch"]
